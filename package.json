{
  "name": "one-bykanvas",
  "private": true,
  "description": "Marketing site and customer accounts for one, by Kanvas.",
  "dependencies": {
    "@supabase/supabase-js": "^2.112.3",
    "stripe": "^17.5.0",
    "@capacitor/core": "^6.2.0",
    "@capacitor/ios": "^6.2.0",
    "@capacitor/android": "^6.2.0",
    "@capacitor/push-notifications": "^6.0.0",
    "@capacitor/browser": "^6.0.0",
    "@capacitor/app": "^6.0.0"
  },
  "devDependencies": {
    "playwright": "1.49",
    "@capacitor/cli": "^6.2.0"
  },
  "scripts": {
    "app:build": "node app/build.js",
    "app:sync": "node app/build.js && npx cap sync",
    "app:ios": "node app/build.js && npx cap sync ios && npx cap open ios",
    "app:android": "node app/build.js && npx cap sync android && npx cap open android"
  }
}
